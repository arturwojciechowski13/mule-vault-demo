# Wypelnij URL swojego repo GitHub (tam, gdzie wypchniesz zawartosc
# tego folderu: mule-app/ + ci/) i uruchom:
#
#   fly -t lab set-pipeline -p mule-app -c ci/pipeline.yml -l ci/vars/repo.yml
#
mule_repo_uri: https://github.com/TWOJ_USER/mule-vault-demo.git
mule_repo_branch: main
